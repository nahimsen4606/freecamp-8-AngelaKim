document.write(1+1)

        let classmate = "철수"                // 변수라서 바꿀수있다
        console. log(classmate)

        classmate = "영희"
        console. log(classmate)

        const aaa = 13 // "13" 얘는 문자 (계산안됨)
        console. log(aaa)

        // aaa = 8
        // console. log(aaa)   <- 상수라서 변경가능하지 않아서 에러난다

        const myMoney = 10000 // 낙타표기법
        console. log(myMoney)